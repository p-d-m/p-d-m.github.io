# Secret 用来保存敏感信息，例如密码、Token、连接串。
# stringData 可以直接写明文，提交到 API Server 后会被转成 data。
# 注意：不要把真实生产密码提交到 Git。
apiVersion: v1
kind: Secret
metadata:
  name: demo-secret
  namespace: demo
type: Opaque
stringData:
  DB_USERNAME: "demo_user"
  DB_PASSWORD: "change-me"
  API_TOKEN: "change-me-token"

